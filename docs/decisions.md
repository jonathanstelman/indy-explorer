# Indy Explorer — Decisions Log

Architectural decisions, design choices, and resolved open questions.
Format for new entries:

---
## YYYY-MM-DD — Short description
**Issue:** #number (if applicable)  
**Decision:** What was decided.  
**Rationale:** Why, and what alternatives were considered.  
**Follow-up:** Open items or gotchas, if any.
---

---
## 2025-02-22 — Resort detail UI
**Issue:** #30  
**Decision:** Opens as a modal, triggered by clicking a map dot or table row.  
**Rationale:** Slide-over panel was considered and rejected.  
**Follow-up:** None — settled.

---
## 2026-03-08 — Rewrite architecture (initial planning)
**Issue:** #51–#55  
**Decision:** Monorepo with `/backend` (FastAPI), `/frontend` (Vite + React), `/pipeline`. `app.py` at repo root stays live on Streamlit Community Cloud during transition.  
**Rationale:** Clean separation of concerns; Streamlit remains the production app until the rewrite is stable.  
**Follow-up:** Remove `app.py` and Streamlit references once rewrite is live.

---
## 2026-03-08 — Resort IDs
**Issue:** #55  
**Decision:** Stable UUID v4 `resort_id` per resort, preserved across pipeline runs via `data/resort_id_map.csv` (columns: `resort_id`, `source`, `source_id`).  
**Rationale:** Indy Pass slugs and row indices change year to year and are unreliable as identifiers.  
**Follow-up:** None — settled.

---
## 2026-03-08 — Map library
**Issue:** #69  
**Decision:** `@deck.gl/react`, not standalone Mapbox GL JS.  
**Rationale:** Supports `onClick` per data point natively, enabling clickable map dots that open the resort detail modal. Pydeck in Streamlit could not do this.  
**Follow-up:** None — settled.

---
## 2026-03-08 — Deployment stack
**Issue:** #54  
**Decision:** Frontend on Vercel, backend on Fly.io (`backend/fly.toml`). Supabase (Postgres + Auth) deferred until data migration and auth are needed.  
**Rationale:** Keeps infrastructure simple during early rewrite phase.  
**Follow-up:** Revisit Supabase when auth or relational queries become necessary.

---
## 2026-03-08 — Data delivery
**Issue:** #55  
**Decision:** `data/resorts.csv` is committed to the repo. FastAPI reads it via `load_resorts()` at startup. The scheduled pipeline (Issue #77) commits the updated file; a new Fly.io deploy picks it up.  
**Rationale:** No external storage or volume mounts needed at this scale.  
**Follow-up:** Revisit if CSV size or update frequency becomes a problem.

---
## 2026-05-17 — Filter state
**Issue:** #64  
**Decision:** URL query params.  
**Rationale:** Users can save and share search results by copying the URL — no auth or saved-search feature needed.  
**Follow-up:** Implement before Issues #14–#18. Ensure filter params are kept clean and human-readable (e.g., `?pass=ikon&region=northeast`).

---
## 2026-05-25 — Mobile sidebar: Drawer overlay pattern
**Issue:** #103, #105
**Decision:** On mobile (< 768px), `AppSidebar` renders as an Ant Design `Drawer` overlay instead of a `Layout.Sider` push column. Desktop keeps the push layout with drag-to-resize. The `isMobile` flag is set on mount and updated on resize. The Drawer is `closable={false}`; the close affordance is a double-chevron SVG in the content header row. The same chevron (in the app header) toggles both desktop and mobile.
**Rationale:** A layout column on mobile squeezed the content area to ~80px, making the footer and table unusable. Overlay keeps content full-width. Ant Design `Drawer` handles the backdrop, animation, and scroll-lock.
**Follow-up:** Touch drag-to-resize on desktop sidebar not implemented (nice-to-have).

---
## 2026-05-22 — Resort detail modal and blackout calendar
**Issue:** #73  
**Decision:** Resort detail opens in an antd `Modal` (600px wide). Blackout dates render in an antd `Calendar` with `fullCellRender` — colored circles over each date (standard=magenta, LTT=cyan, both=diagonal split gradient). Today gets a border ring only (transparent fill so blackout color shows through). Out-of-month dates fade to 10% opacity. Month state persists across modal opens via a module-level variable (`sharedCalendarMonth`) that survives `destroyOnClose` unmounting. All three navigation paths (dropdowns, prev/next buttons, clicking an out-of-month date) route through the same `setMonth()` setter, which also updates the shared variable.  
**Rationale:** `fullCellRender` replaces the entire cell (vs. `cellRender` which appends below the date number, causing double rendering). Module-level variable chosen over context/localStorage to avoid prop drilling with no persistence requirement across page reloads. Color pair (magenta + cyan) selected for sufficient contrast against each other and against white text/dark text respectively.  
**Follow-up:** None — settled.

---
## 2026-05-22 — Resort detail charts and Peak Rankings (#74, #75)
**Issue:** #74, #75  
**Decision:** Added three chart/stat sections to the resort detail modal. Elevation shown as a vertical stat stack (Summit/Drop/Base) with a cyan left-border accent — no chart, since any bar would imply proportionality from sea level that's visually misleading. Trail difficulty uses a Recharts PieChart with the legend to the left; colors follow ski trail convention (green `COLORS.difficultyBeginner` / cyan / graphite `COLORS.textMuted`). Snowfall is a simple two-bar Recharts BarChart (average vs. max). Peak Rankings (#75) is pure data: total score + ranks prominently at top, 10 category scores in a 5-column grid with mini progress bars (0–10 scale), and an extras grid for lodging, après, access road, ability range, nearest cities, and pass affiliation. Recharts added as a dependency.  
**Rationale:** Elevation chart approaches (stacked bar, waterfall) all imply sea-level proportionality; stat boxes are unambiguous. Difficulty colors chosen for colorblind accessibility — green/cyan/graphite separate by both hue and value. Peak Rankings section hidden entirely when pr_total is null.  
**Follow-up:** None — settled.

---
## 2026-05-23 — Resort data table implementation (#72)
**Issue:** #72  
**Decision:** AG Grid Community Edition (v35) with explicit `width` on every column in `COLUMN_DEFS`. No auto-sizing.  
**Rationale:** `autoSizeAllColumns()` is unreliable in v35 — inflates columns with long headers disproportionately to data density. `maxWidth` in `defaultColDef` initializes all columns to that value rather than acting as a ceiling. Explicit widths tuned per-column are the only reliable approach at this version.  
**Follow-up:** Revisit auto-sizing if AG Grid fixes the behavior in a future release.

---
## 2026-05-23 — Table toolbar lifted to App.jsx drag handle
**Issue:** #72  
**Decision:** Table controls (Select Columns popover, Download CSV, Full screen) live in App.jsx's drag handle row, not inside ResortTable. ResortTable is a `forwardRef` component; App.jsx holds `gridRef` and calls AG Grid APIs directly. `COLUMN_DEFS`, `HEADER_BY_FIELD`, and `COL_GROUPS` are named exports from ResortTable.jsx.  
**Rationale:** Consolidating the drag handle and toolbar into one row eliminates a redundant UI strip and removes duplicate search bars (the table quick-filter conflicted with the sidebar search). The `forwardRef` pattern keeps ResortTable a pure grid renderer with no toolbar concerns.  
**Follow-up:** None — settled.

---
## 2026-05-24 — Pipeline backup strategy and data deployment approach
**Issue:** #76, #77
**Decision:** Pipeline backs up `resorts.csv`, `resort_id_map.csv`, and `pipeline_metadata.json` to `backups/YYYY-MM-DDTHH-MM-SS/` before any steps run, keeping the 10 most recent. Cache HTML is excluded (large, regenerable). Naming convention uses ISO 8601 with hyphens replacing colons. Automated pipeline (#77) will open a PR rather than auto-committing to main — human reviews the data diff before merging. Pre-PR sanity check runs `load_resorts()` against the new CSV; requires #83 (Pydantic validation) first.
**Rationale:** App seasonality means bad data could go unnoticed for months. PR-based promotion gives a human gate without requiring a staging environment. Backup-before-run gives a fast local restore path without touching git history.
**Follow-up:** #83 must be completed before #77's validation step is implemented.

---
## 2026-05-23 — Centralized color system in theme.js
**Issue:** #72 (polish)  
**Decision:** All color values in `frontend/src/` must reference named tokens from `COLORS` in `src/theme.js`. No hardcoded hex or rgba strings in `.jsx` or `.css` files. CSS files bridge to theme values via CSS custom properties set in `GRID_THEME_VARS` (e.g. `--indy-header-bg`). The `withAlpha(hex, alpha)` utility generates rgba strings from theme hex values.  
**Rationale:** One audit found `COLORS.accent` (undefined), two unique greens, and three similar greys scattered across components. Centralizing prevents drift and makes visual changes a single-file edit.  
**Follow-up:** When adding new colors, add to `COLORS` in theme.js first, then reference by name.

---
## 2026-05-24 — Deployment: Fly.io + Vercel split
**Issue:** #100  
**Decision:** Backend on Fly.io (`indy-explorer-backend`, region `ewr`), frontend on Vercel. Frontend proxies `/api/:path*` → `https://indy-explorer-backend.fly.dev/:path*` via `vercel.json` rewrite — no CORS config needed. Docker image built from repo root so `data/` is baked in at deploy time (not read from a volume).  
**Rationale:** Vercel is purpose-built for static SPAs (CDN, zero-config GitHub auto-deploy). Fly.io handles the containerized FastAPI. All-Fly.io was considered but requires nginx and has no global CDN equivalent. Baking `data/` into the image keeps the backend stateless and dead-simple to deploy; after a pipeline data update, redeploy the backend.  
**Follow-up:** After a pipeline data update, both `data/resorts.csv` must be committed to main AND the backend must be redeployed (`flyctl deploy --config backend/fly.toml`) for new data to appear in the live app.

---
## 2026-05-25 — Keep Fly.io machine warm to eliminate cold starts
**Issue:** #106
**Decision:** Set `min_machines_running = 1` in `backend/fly.toml` (was 0). One machine runs continuously instead of spinning down on idle.
**Rationale:** With `min_machines_running = 0`, the first request after any idle period incurred a ~2–3s cold start — bad first impression for Facebook link clicks. Cost is ~$3–4/month in prepaid Fly.io credits.
**Follow-up:** None.

---
## 2026-05-31 — UI polish batch: tooltips, sidebar filters, mobile chrome
**Issue:** #116 / #117
**Decision:** Map tooltip reduced from 14 rows to 6 (Resort, Location, Acres, Vertical, Trails, Lifts); US resorts omit country from Location. Sidebar "Blackout Dates" section renamed "Planning" and absorbs Reservation Required; sub-headings added for Lift Ticket / Learn to Turn. LTT expanded to "Learn to Turn" throughout. New Yes/No toggles for "Has Peak Rankings" and "Blackout dates" (`has_blackouts` backend param added). `FeatureToggle` exported and reused rather than duplicated. Sticky section headers added to sidebar. Mobile chrome slimmed (header 56→40px, tab bar 48→36px). Playwright added as frontend dev dependency for UI verification.
**Rationale:** Tooltip was overwhelming quick-glance UX — detail belongs in the modal. "Planning" groups logistically-related filters (reservations, blackouts) that were scattered. LTT is not a known initialism outside Indy Pass context. Mobile chrome consumed ~30% of screen on small phones.
**Follow-up:** `has_blackouts` Yes/No toggle for Learn to Turn blackouts not added (would require separate backend param — deferred).

---
## 2026-07-11 — Popup/popover design language: unified dark-border panel
**Issue:** #110
**Decision:** All popup surfaces (feedback footer, mobile ⓘ, Select Columns) use a consistent pattern: `2px solid COLORS.bgHeader` border, `borderRadius: 4`, no arrow, dark `COLORS.bgOverlay` overlay that dismisses on click. Desktop "Select Columns" and footer feedback use `position: fixed` centered panels; mobile panels are `position: absolute` above the tab bar. antd Popover dropped for these surfaces in favor of hand-rolled divs to guarantee consistent styling. Popup content uses colored dot + section title (matching sidebar), bulleted `COLORS.text` links, smaller muted footnotes (`fontSize: 10`).
**Rationale:** Each surface was previously bespoke (antd Popover with overrides, custom divs, different borders/shadows). Unifying removes visual drift and makes future changes easier. antd Popover's placement logic was unreliable on short viewports — fixed centering is more predictable.
**Follow-up:** #120 tracks extracting these into shared primitives so the pattern doesn't have to be re-implemented per surface.

---
## 2026-05-25 — Theme color pass: 80s ski-punk palette
**Issue:** #107
**Decision:** Expanded color palette in `theme.js` with `accentBlue` (#3d52ff), `accentPurple` (#9b00e6), `bgMidtone` (#505050), deepened `primary` to #00c4d4 and `success` to #b4f000 (chartreuse). Map dots: pink=alpine, electric blue=XC, purple=both (pink+blue=purple). Difficulty pie chart: chartreuse=beginner, blue=intermediate, grey=advanced (matches real trail markers). PR bars colored by score value (green/yellow/pink). Dark anchor bands: near-black header, charcoal search band and table drag handle, charcoal table column headers. Features section redesigned as "Label: Yes/No" grid. Neon colors used on dark surfaces; functional colors carry semantic meaning throughout.
**Rationale:** Original theme used only hot pink and pale cyan; neons were invisible on white. Key insight: neons only work on dark surfaces — structural dark bands provide the canvas. Color choices are functional (map dot mixing, trail marker convention, traffic-light PR scores) not purely decorative.
**Follow-up:** Dark mode (future) will unlock full neon palette across the whole surface.

---
## 2026-07-11 — Retire Streamlit app; replace with redirect page
**Issue:** #121
**Decision:** `app.py` replaced with a minimal Streamlit redirect page (~17 lines) and moved to `legacy/app.py` (along with `.streamlit/config.toml` → `legacy/.streamlit/config.toml`). All 1480 lines of Streamlit app code removed. `README.md` rewritten to present the React/FastAPI stack as the primary app. `CLAUDE.md` updated to describe `legacy/app.py` as "redirect notice only." Button color set to `accentBlue` (#3d52ff). Page content centered via CSS injection.
**Rationale:** React app has full feature parity and loads instantly (no cold start). Streamlit had a 5–10s cold start that was the main user complaint. Keeping `app.py` as a live redirect rather than deleting it preserves the Community Cloud URL so existing bookmarks and shared links forward correctly.
**Follow-up:** After redeploying on Streamlit Community Cloud, confirm the live redirect works before merging this PR.

---
## 2026-07-11 — Playwright MCP output directory
**Issue:** #124
**Decision:** Playwright MCP artifacts (screenshots, accessibility snapshots, console/network logs) are scoped to `.playwright-mcp/` at the repo root, which is now gitignored. `CLAUDE.md` documents the convention: pass `filename` as `.playwright-mcp/<name>.png` on `browser_take_screenshot` calls.
**Rationale:** A screenshot taken with a bare filename landed at the repo root as untracked cruft. `.playwright-mcp/` was already the tool's default location for snapshot/console log files, so it was reused rather than introducing a second directory.
**Follow-up:** None — settled. Confirmed Playwright MCP works end-to-end against the local dev servers (navigate, snapshot, screenshot) once a macOS system-permission prompt was cleared.

---
## 2026-07-11 — Shared Panel/Overlay primitives + unified dim-focus tiers
**Issue:** #120
**Decision:** Extracted `frontend/src/components/common/Panel.jsx` (bordered box, optional close button, exports `PANEL_STYLE` for antd consumers) and `Overlay.jsx` (dismiss-on-click dim layer, exports `OVERLAY_Z_INDEX`/`PANEL_Z_INDEX`). Applied across Select Columns and mobile ⓘ (App.jsx), the Feedback popover (Footer.jsx), Map Legend/Attribution (ResortMap.jsx), and the map hover tooltip. Settled on two dim tiers: **ambient** (no dim — Feedback, Map Legend, Map Attribution/desktop ⓘ) and **soft** (~45% — How To Use, Resort Detail, Select Columns, mobile ⓘ). `COLORS.bgOverlay` changed from `rgba(0,0,0,0.75)` to `rgba(0,0,0,0.45)` to match antd's default modal mask.
**Rationale:** Before touching code, audited actual dim behavior with pixel-sampled screenshots (not just visual inspection — a genuine leak, described below, was invisible to the eye but showed up clearly in RGB deltas) across desktop/mobile × sidebar-expanded/collapsed. Found real inconsistencies: Feedback and Map Legend/Attribution had 0% dim, Select Columns had 75% dim with a bug — the sidebar's Collapse section headers all set `zIndex: 10` (`Sidebar.jsx`) against the overlay's `zIndex: 8`, so every section header (not just the expanded one) punched through undimmed — and mobile ⓘ had 75% dim vs. its desktop equivalent's 0%. User's call: 75% read as overdone for anything, so both "should dim" surfaces converge on one soft value instead of a 3-tier system.
**Follow-up:** Fixed the leak at the source — `Sidebar.jsx`'s `stickyHeader` zIndex dropped from 10 to 1, since it only needs to clear its own scrolled-under content locally, not app-wide overlays. Follow-on fixes to the map tooltip and the two full-page modals are logged separately below.

---
## 2026-07-11 — Map tooltip clipping fix, and full-modal border consistency
**Issue:** #120
**Decision:** The map hover tooltip (`MapTooltip` in `ResortMap.jsx`) now clamps its position within the map container instead of using CSS `flip`-to-opposite-side math (`bottom: calc(100% - y + 8)`), and adopts `Panel`'s bordered style like the Legend/Attribution it shares the map with. `onHover` now computes clamped `left`/`top` pixel values directly, bounded to `[4, container size − tooltip size − 4]` on both axes. `ResortDetailModal` was reworked to match `HowToUseModal`'s already-correct viewport-constrained layout (fixed dark header, independently scrollable body capped to `calc(100vh - ...)`, mobile full-screen) instead of only capping the inner `body` to `85vh` with no cap on the modal as a whole. Both `HowToUseModal` and `ResortDetailModal` now also get a `2px solid COLORS.bgHeader` border around the whole modal (desktop only — mobile stays edge-to-edge), matching the small-popup border language established above, layered outside the existing 2px white inset frame (not replacing it — a border-only version read as a flat double-ring; user preferred keeping the white gap between the border and the dark header).
**Rationale:** The tooltip clip was a real, if narrow, bug: `Layout.Content` has `overflow: hidden` (App.jsx), and when the map area is short (table expanded) and a hovered dot sits near the container's bottom edge, the flipped tooltip's calculated top could land above `Layout.Content`'s own top edge and get silently clipped, hiding the "Resort"/"Location" rows. Diagnosed via `elementFromPoint` first, which was a dead end — the tooltip's `pointer-events: none` makes hit-testing skip it regardless of visual paint order — then confirmed via ancestor `overflow`/rect inspection and pixel-level before/after screenshots. `ResortDetailModal` had the same class of bug (only `body` was height-capped, not the modal as a whole) plus no border at all, both caught by the user directly. Bold borders on the two full modals were a deliberate design call (not just consistency): the app's "bold, high-contrast" aesthetic direction (`docs/design.md`) favors it over a plain white antd edge, at the cost of modals losing a bit of "bigger, different kind of surface" distinction from the small popups.
**Follow-up:** None — settled.

---
## 2026-07-11 — Map area now clips its own overlays (Legend/Attribution/Tooltip)
**Issue:** #120
**Decision:** The desktop and mobile map wrapper `div`s in `App.jsx` gained `overflow: hidden`. `MapLegend` and `MapAttribution` (`ResortMap.jsx`) gained `maxHeight: calc(100% - 16px)` + `overflowY: auto` so they scroll internally rather than overflowing when the map area is short.
**Rationale:** With a short map area (table expanded), the Legend panel — anchored `top: 8, left: 8` with no height constraint — could grow taller than the map itself and visually spill onto the "Hide data table" drag-handle bar below it, a section the user correctly identified as not part of the map. Since the map wrapper had no `overflow` set, nothing stopped an absolutely-positioned child from rendering past its bottom edge into the next flex sibling. The fix contains the map's own decorations to the map's own box — the same principle as the tooltip clamp above, applied to Legend/Attribution, which didn't need clamping before because their fixed anchor position was never tested against a short container.
**Follow-up:** None — settled.

---
## 2026-07-11 — Select Columns gets the same header/border treatment as full modals
**Issue:** #120
**Decision:** The Select Columns panel (`columnsPanel()` helper in `App.jsx`, used by both the desktop centered popup and the mobile above-tab-bar popup) now has a dark `bgHeader` title bar ("SELECT COLUMNS") with a close button, matching `HowToUseModal`/`ResortDetailModal`'s header — including the same 2px white inset frame between the outer border and the header (`padding: 2` on the `Panel`, not just a flush border). Desktop width increased from 340px to 440px. The panel is now a flex column (`Panel` sets `overflow: hidden`; an inner `div` handles the independent scroll), same structural pattern as the two full modals.
**Rationale:** The user pointed out the panel felt cramped (dense 2-column checkbox groups in a 340px box) and had no title at all — no context for what the popup was if you saw it out of context. It's the most content-heavy of the small popups, so it made sense to borrow the full-modal treatment rather than stay in the bare small-popup style used by Feedback/Legend/Attribution/tooltip, which are much lighter-weight by comparison. Applied to mobile too, per explicit confirmation, despite the tighter vertical space.
**Follow-up:** None — settled.

---
## 2026-07-11 — Extracted ModalHeader/ModalShell: the big-surface pattern was copy-pasted, not shared
**Issue:** #120
**Decision:** New `frontend/src/components/common/ModalHeader.jsx` (title/subtitle/onClose/titleFontSize → the dark header bar + close button) and `ModalShell.jsx` (wraps antd `Modal` with the viewport-constrained desktop/mobile sizing: bold border + white inset frame on desktop, full-bleed on mobile). `HowToUseModal.jsx` and `ResortDetailModal.jsx` now both use `ModalShell` + `ModalHeader` instead of independently duplicating the same ~20-line `Modal` `styles` block and the same header markup. `App.jsx`'s `columnsPanel()` now uses `ModalHeader` inside its `Panel` wrapper instead of a third hand-copy of the header. `ModalShell` also replaces the old magic-number `calc(100vh - ${gap*2+60}px)` / `+76` height estimates with a flex-column layout (`container: {display:'flex', flexDirection:'column', maxHeight: calc(100vh - gap*2)}`, body `flex:1, minHeight:0`) — the header is `flexShrink:0` and content is `flex:1, overflowY:auto`, so it self-sizes correctly regardless of header height instead of needing a hand-tuned pixel constant per modal.
**Rationale:** Prompted by the user directly asking whether the modal work was actually DRY. It wasn't — three files independently reimplemented the same dark-header-bar-with-close-button chrome, and two of them duplicated the entire antd `Modal` viewport-constraint config byte-for-byte (only width and a `destroyOnHidden` flag differed). Verified no visual regression: screenshotted all three surfaces (desktop + mobile) before and after and confirmed pixel-identical output.
**Follow-up:** None — settled.

---
## 2026-07-12 — Alpine + XC metrics captured as separate `_xc` fields
**Issue:** #11
**Decision:** `parse_resort_page()` now also reads the `-xc` suffixed DOM elements (`field--name-field-trails-xc`, `field--name-field-{beginner,intermediate,advanced}-xc`) into new keys `trails_xc` / `difficulty_*_xc`, defaulting to `None` when absent. `prep_resort_data.py` outputs them as four new columns (`num_trails_xc`, `difficulty_beginner_xc`, `difficulty_intermediate_xc`, `difficulty_advanced_xc`), with a defensive fill so prep won't crash on extracts predating the XC-aware scraper. Backend: fields added to `Resort`/`ResortSummary`, `min_trails_xc`/`max_trails_xc` range filters, and a `num_trails_xc` range in `/meta`. Frontend: discipline mode (`'alpine' | 'xc' | 'both'`) is derived purely from the existing `has_alpine`/`has_cross_country` filters via `useDisciplineMode()` — no new state or toggle. `utils/fieldVariants.js` is the single source for the base→`_xc` field mapping (`resolveField(resort, baseField, { discipline })`, options object kept extensible for a future units axis) and `fieldHiddenByMode()`. Sidebar gains a `Trails (XC)` slider; the alpine Trails slider hides in XC mode and vice versa. The table gains 4 XC columns; mode narrows displayed columns declaratively (memoized `columnDefs` with `hide`) layered on top of Select Columns state without mutating it — imperative `applyColumnState` calls removed. Map tooltip shows the trails row(s) per mode (height clamp adjusts for the 7-row 'both' case). The detail modal is deliberately NOT mode-aware: XC trails stat and a second `DifficultyChart` ("Trail Difficulty (XC)") always render below the alpine versions when the resort has XC data, mirroring the Indy Pass page layout.
**Rationale:** Root cause was BeautifulSoup's exact-token class matching silently never finding the `-xc` variants, so mixed alpine+XC resorts showed only alpine stats and XC-only resorts showed nothing. Purely additive by design: `num_trails`/`num_lifts` deliberately stay sourced from the main resort-listing page (existing "defer to main page" decision confirmed still correct) and alpine difficulty fields are untouched. No filter params for `difficulty_*_xc`, matching alpine difficulty being display-only.
**Follow-up:** `data/resorts.csv` must be regenerated by the pipeline (and the backend redeployed — Docker bakes `data/` at build time) before the new columns serve real values. Playwright visual verification pending.

---
## 2026-07-12 — Clear hidden Trails filters on discipline-mode change
**Issue:** #11
**Decision:** `filtersToClearForMode(mode)` (new pure export in `useDisciplineMode.js`) returns the URL params to null out for a given resulting mode: `{ min_trails, max_trails }` when mode becomes `'xc'`, `{ min_trails_xc, max_trails_xc }` when mode becomes `'alpine'`, `{}` when it becomes `'both'`. `FeatureFilters.jsx`'s `FeatureToggle` special-cases the `has_alpine`/`has_cross_country` keys (`DISCIPLINE_KEYS`): on click it computes the *resulting* filter state and resulting mode synchronously, then calls a single `setFilters({ [filterKey]: next, ...filtersToClearForMode(resultingMode) })` — one atomic URL update instead of a toggle followed by a separate clearing effect. Every other boolean filter keeps the original plain `setFilter()` path.
**Rationale:** A hidden Trails slider must not leave its range filter silently active — the user would see fewer results than expected with no visible control explaining why. Computing off the *resulting* state (not the current one) and applying both changes in one `setFilters()` call avoids a stale-closure/extra-render gap where a between-render read of `filters` could see the pre-toggle mode.
**Follow-up:** None — settled.

---
## 2026-07-12 — Discipline toggles surfaced as an always-visible "Mode" section
**Issue:** #11
**Decision:** `has_alpine`/`has_cross_country` moved out of `FeatureFilters.jsx`'s `BOOLEAN_FILTERS` list into a new `frontend/src/components/filters/DisciplineFilter.jsx`, rendered in `Sidebar.jsx`'s shared `content` const between the "Reset all filters" row and the `Collapse` — outside any accordion panel, so it's always visible on both desktop (`Layout.Sider`) and mobile (`Drawer`) with no `isMobile` branching. `DisciplineFilter` reuses the exported `FeatureToggle` unchanged (same `DISCIPLINE_KEYS` atomic-clear behavior from the prior entry), adds a one-line caption ("Also controls which stats are shown below"), and a read-only "Viewing: Alpine / Cross-Country / Both" text readout derived from `useDisciplineMode()` — not a new control, just visible confirmation of the derived mode. `Sidebar.jsx` labels the section with the existing `sectionLabel()` dot pattern (`COLORS.accentPurple`, matching the map's alpine+XC "both" dot color) and a border separator before the `Collapse` begins, to read as structurally different from the collapsible filter categories below it.
**Rationale:** Caught by visually inspecting the running app: the two discipline toggles looked identical to unrelated filters like "Night skiing," giving zero indication that toggling them also hides/shows the Trails vs Trails (XC) sliders, table columns, and map tooltip rows elsewhere in the app. Relocating (not duplicating) them and adding the caption + live readout makes the dual-purpose behavior visible instead of implied.
**Follow-up:** Mobile placement (rendering inside the `Drawer`) is the simplest correct thing for now but not a settled design decision — the user may revisit whether "always visible" is right in the mobile/Drawer context in a later pass. Superseded by the next entry: the underlying design (overloading `has_alpine`/`has_cross_country` with a display-mode side effect) turned out to be the root problem, not just the toggles' visibility.

---
## 2026-07-12 — Split `has_alpine`/`has_cross_country` from display mode entirely: new independent `view` filter
**Issue:** #11
**Decision:** Reverses the two prior entries' approach after seeing it live: `has_alpine`/`has_cross_country` go back to being plain, ordinary boolean resort-inclusion filters — moved back into `FeatureFilters.jsx`'s `BOOLEAN_FILTERS` list, `FeatureToggle` loses the `DISCIPLINE_KEYS` special-case entirely (back to a single unconditional `setFilter()` path for every entry). Display mode is now its own genuinely independent URL filter, `view` (`'alpine' | 'xc' | 'both'`, default `'both'`, never written to the URL when at default — same "null out the default" convention `RangeSlider` already uses). `useDisciplineMode.js` (which derived a mode from the two booleans) is deleted outright; `frontend/src/hooks/useView.js` replaces it with `useView()` (thin read of `filters.view`) and `filtersToClearForView(view)` (renamed from `filtersToClearForMode`, same clearing behavior, now triggered by `view` changing instead of a toggle). New shared `frontend/src/components/filters/ViewSelector.jsx` (antd `Segmented`, 3 options) is the single control, rendered in two places that stay in sync automatically because both read/write the same `view` param: `DisciplineFilter.jsx` (sidebar "Mode" section, now just a caption + `<ViewSelector />`, no more `FeatureToggle`s or read-only "Viewing: X" text — the selected segment is the live indicator) and `ResortToolbar.jsx` (right-aligned via `marginLeft: 'auto'`, added since the row had no existing right-hand slot). `StatsFilters.jsx`, `App.jsx` (column visibility), `ResortMap.jsx` (tooltip rows) now read `view` via `useView()` directly instead of a derived mode. `resolveField`/`fieldHiddenByMode` in `fieldVariants.js` needed no logic changes — already keyed by a plain discipline string, callers just pass `view`. `ResortDetailModal.jsx` untouched (was never discipline-mode-aware).
**Rationale:** Discussed with the user after seeing the toggle-plus-caption-plus-readout UI live: `has_alpine`/`has_cross_country` were doing two unrelated jobs — filtering which resorts appear vs. controlling which stats display — and every attempt to make that clear in the UI (caption, live readout, dedicated section) kept feeling awkward because the underlying data model still conflated them. Splitting into genuinely independent state removes the seam instead of explaining around it: a resort filtered out by `has_cross_country=false` and a sidebar showing only XC stats are now two separate, orthogonal decisions a user can make.
**Follow-up:** None — settled. Tests updated to match the simpler contract: `useDisciplineMode.test.js` deleted, `useView.test.js` covers only `filtersToClearForView` (no more mode-derivation tests, since there's nothing left to derive), `fieldVariants.test.js` dropped its `deriveDisciplineMode` describe block.

---
## 2026-07-12 — `ViewSelector` polish: compact teal styling, mobile toolbar wrap, collapsible sidebar Mode section
**Issue:** #11
**Decision:** Four presentational fixes verified live via Playwright at multiple viewports. (1) `ViewSelector.jsx`'s XC option label shortened from "Cross-Country" to "XC" (map legend keeps the full "Cross-country" wording — only the compact control changed). (2) New `theme.js` `components.Segmented` entry (`SEGMENTED_TOKENS`) — matches the established `Input`/`Select`/`Button` component-token override pattern already in `themeConfig.components` — sets `itemSelectedBg: COLORS.primary` (teal) and `itemSelectedColor: COLORS.text` for the selected pill, plus `trackPadding`/`controlHeightSM`/`fontSize` overrides for a size below antd's built-in `size="small"`. (3) `ResortToolbar.jsx`'s row gained `flexWrap: 'wrap'` and `Input.Search` gained `minWidth: 140` (previously unset) — confirmed via Playwright at 375/390/428px that without a floor, unconstrained flex-shrink collapsed the search box down to ~1 character wide (worse: to a single visible letter) before wrapping ever kicked in; now the input holds a readable minimum width and `ViewSelector` (`marginLeft: auto`) drops to its own right-aligned row instead. Desktop (1280px) still renders as a single row, confirmed unaffected. (4) Sidebar's "Mode" section moved from a permanently-visible block outside `Collapse` into the `items` array like the other four sections — now genuinely collapsible, with a dynamic header (`Mode: Alpine` / `Mode: XC` / `Mode: Both`, sourced from a new `VIEW_LABELS` export in `ViewSelector.jsx` so the header text/capitalization never drifts from the control itself) and `'mode'` added to `ALL_KEYS` so it's expanded by default, matching prior always-open behavior.
**Rationale:** The stale comment forcing Mode to render outside `Collapse` (from the pre-pivot design where the toggles had a hidden side effect) no longer applied once `view` became independent filter state — collapsibility is now just consistent behavior, not a UX risk. The toolbar wrap issue was caught by literally resizing the browser via Playwright rather than assuming the `marginLeft: auto` layout from the prior turn was safe — it silently broke the search box, not the originally-suspected `ViewSelector` clipping.
**Follow-up:** Superseded same-day by the next entry — the `view`/mode concept this polish was refining was itself removed after realizing the scope didn't justify it. See below.

---
## 2026-07-12 — Removed the `view`/mode concept entirely: scope didn't justify it
**Issue:** #11
**Decision:** Reverses same-day work (the independent `view` filter, `ViewSelector`, `DisciplineFilter`, `useView.js`, `fieldVariants.js`, and all mode-based hiding in `StatsFilters`/`App.jsx`/`ResortMap.jsx`) after realizing the real scope was one filterable field (`num_trails_xc`) and three display-only ones (`difficulty_*_xc`, modal-only) — not enough to justify a global display-mode concept with its own state, two synchronized controls, and mode-aware hiding across three components. Final shape: `Trails (XC)` is an ordinary always-shown sidebar slider next to `Trails`; `num_trails_xc`/`difficulty_*_xc` are plain optional table columns via the existing Select Columns mechanism, no different from any PR score or blackout column; the map tooltip's `Trails (XC)` row is a per-resort presence check (`resort.num_trails_xc != null`), same pattern the detail modal already used from turn one. `has_alpine`/`has_cross_country` stay as the plain boolean filters they were reverted to two turns ago. Deleted: `useView.js`+test, `ViewSelector.jsx`, `DisciplineFilter.jsx`, `fieldVariants.js`+test, the `view` URL param, the `Segmented` theme override, the sidebar Mode section, the toolbar's second control. `App.jsx`'s declarative memoized `columnDefs` (introduced during the mode work, fixed a real pre-existing bug where Select Columns state was lost on grid remount) was kept — only the `view`-based branching inside it was stripped, not the mechanism itself; manually verified via Playwright that toggling a column and collapsing/re-expanding the table still preserves the choice.
**Rationale:** Right-sizing the solution to the actual data shape, not the shape a "toggle" framing implied — the moment mode existed, the codebase accreted a control, a URL param, and hiding logic in three places to serve one number. Simpler was correct once the real scope was named.
**Follow-up (reusable patterns worth keeping even though this code is gone):**
- A single control can be rendered in multiple locations (sidebar + toolbar) by having both instances read/write the *same* shared state directly — no read-only mirror or duplicated logic needed, as long as it's genuinely one piece of state, not two kept in sync by hand.
- A collapsible section header can surface its current selection in the header text itself (e.g. `Mode: Alpine`) so the choice stays visible even when collapsed — worth reusing for other sidebar sections with a meaningful current-value summary.
- antd `Segmented` is the right primitive for a small, mutually-exclusive choice — used it after first trying two independent Yes/No button pairs, which was the wrong shape for "pick one of three."
- Methodology, most important: before choosing between an exclusive-choice control (segmented/radio) and an always-show-both approach, explicitly ask whether a user would ever want multiple options visible *simultaneously*. The "family planning a trip wants to see both disciplines at once" case is what invalidated the mode-toggle approach entirely — asking that question up front would have skipped the whole detour.
- Separately (still true, unrelated to the above): the Select Columns picker's two-column layout (`display: grid, gridTemplateColumns: '1fr 1fr'` in `App.jsx`) has *default row-wise* auto-flow, not column-wise — a flat `COL_GROUPS` field list must interleave paired fields (`base, base_xc, base2, base2_xc, ...`) for them to land side by side in the same row. `ResortTable.jsx`'s `Size`/`Difficulty` groups were reordered this way (confirmed visually: Difficulty now renders alpine fully in the left column, XC fully in the right; Size pairs `Trails`/`Trails (XC)` in one row) — worth remembering next time a two-column picker group looks scrambled despite the source array "looking grouped."

---
## 2026-07-12 — Trail Length columns relabeled as XC-specific
**Issue:** #11
**Decision:** `ResortTable.jsx`'s `headerName` for `trail_length_mi`/`trail_length_km` changed from `'Trail Length (mi/km)'` to `'Trail Length XC (mi/km)'` — matching the `Beginner XC (%)` convention (XC placed before the unit parenthetical) since these fields already carry a unit, rather than the `Trails (XC)` convention. Field names, backend model, and pipeline untouched — display-label-only change. `HEADER_BY_FIELD` (derived from `COLUMN_DEFS`) is the single source for both the table headers and the Select Columns picker labels, so one edit covers both surfaces; confirmed live via Playwright.
**Rationale:** Verified against the actual dataset that `trail_length_mi`/`trail_length_km` are populated in 0 of 221 alpine-only resorts, 88% of XC-only resorts, and 46% of mixed resorts — a Nordic "total groomed km" convention, not a generic resort-wide metric as the unlabeled column implied. Misleading now that the column sits directly next to the genuinely-paired `Trails`/`Trails (XC)` columns from the rest of this issue.
**Follow-up:** The same unlabeled "Trail length (mi)" wording also appears in the sidebar `StatsFilters.jsx` slider and the `ResortDetailModal.jsx` Size stat — out of scope for this fix (explicitly scoped to `ResortTable.jsx` only) but worth the same relabel in a future pass for consistency.

---
## 2026-07-12 — Final pre-PR pass: fixed a real regression, closed the relabel follow-up, suppressed a misleading duplicate row
**Issue:** #11
**Decision:** Four fixes applied directly (not via the coordinating agent, to conserve budget) while reviewing the finished branch in a browser: (1) `ResortMap.jsx` had two leftover references to the deleted `view` variable (`tooltipHeight(view)` in the `onHover` handler and a `view={view}` prop passed to `MapTooltip`, which no longer accepts one) — a `ReferenceError` thrown on every hover that silently killed the tooltip entirely, missed by the test suite and `npm run build` since it's a runtime-only error in a callback with no component-test coverage. (2) Completed the prior entry's flagged follow-up: `StatsFilters.jsx`'s slider label and `ResortDetailModal.jsx`'s Size stat both relabeled to `Trail Length XC (mi)`. (3) The `Trails (XC)` row (map tooltip and detail modal) is now suppressed for XC-only resorts (`has_alpine === false`) — verified against all 38 XC-only resorts with `num_trails_xc` populated that `num_trails === num_trails_xc` in every case, since both numbers describe the same single trail network from two independent sources; showing both read as a duplication bug even though it was accurate. Mixed resorts (e.g. 49 Degrees North, where Indy Pass's own site independently lists "88" in both the alpine and `-xc` DOM elements) still show both rows unmodified — confirmed against the raw source HTML that this is Indy Pass's own data, not a scraping artifact, and per this project's "represent source data faithfully" principle it's left alone rather than guessed at. (4) Diagnosed and fixed a dev-environment-only issue, not an app bug: `backend/main.py` loads `_resorts = load_resorts()` once at process start (module-level), not per-request, so a `uvicorn --reload` dev server (which only watches `.py` files) kept serving a stale pre-XC-data snapshot through several rounds of regenerating `data/resorts.csv` — required a manual server restart, not a code change.
**Rationale:** Caught by actually driving the app in a browser rather than trusting green tests/build — exactly the gap CLAUDE.md's Playwright verification step exists to catch, and this session found two real cases (the `ReferenceError` and the stale backend snapshot) that no test in this repo's suite would have caught.
**Follow-up:** None — settled. All three suites (pipeline pytest, backend pytest, frontend vitest) and `npm run build` reconfirmed green after these fixes.

---
## 2026-07-12 — AG Grid Theming API migration; real prior styling was mostly broken
**Issue:** #118
**Decision:** `ResortTable.jsx` migrated from AG Grid's legacy CSS-file theming (`ag-grid.css`/`ag-theme-quartz.css` imports + `className="ag-theme-quartz"` + `--ag-*` CSS custom properties) to the Theming API (`themeQuartz.withParams(...)` passed via the `theme` prop) — clears AG Grid console error #239 (Theming API and CSS File Themes both in use). `ResortTable.css` deleted entirely; header background/hover/text color and font-weight, previously plain CSS class overrides, are now native theme params (`headerBackgroundColor`, `headerCellHoverBackgroundColor`, `headerTextColor`, `headerFontWeight`). Investigated via `git stash` + `getComputedStyle` (not just visual screenshot comparison, which was misleading twice this session due to mismatched sidebar-expand state and an unverified underline claim) to establish the actual pre-migration rendered values as ground truth, since the issue only mentioned font-family being silently defeated by the theme conflict. Found every `--ag-*` variable in the old `GRID_THEME_VARS` was being silently discarded, not just font-family: row hover was `transparent` (no visible feedback at all), selection was AG's default gray `#bbb`, border was `rgba(0,0,0,0.25)`, font was 14px `"Helvetica Neue"` (not the intended 12px mono), padding was 11px (not 8px), rows were 25px (compact). Only the header background/hover/text survived, because those were set via plain CSS class selectors in `ResortTable.css`, not `--ag-*` variables. `GRID_THEME` rebuilt to match this real prior look (`fontSize: 14`, `rowHeight`/`headerHeight: 25`, `cellHorizontalPadding: 11`, `wrapperBorderRadius: 0`, `columnBorder: false`, `headerColumnBorder: false`, `headerColumnResizeHandleColor: 'transparent'`) rather than the never-actually-applied intended design. New `FONTS.sans` token (`theme.js`) replaces the default's unrelated "IBM Plex Sans" web font with a system-sans stack matching what was actually rendering.
**Rationale:** User's explicit bar for this issue was "if anything changes visually, it's a regression" — but the real baseline had several silently-broken bits (no row hover, near-invisible dark borders) that weren't worth perfectly reproducing once found. Discussed and settled: row hover (previously invisible) becomes a deliberate, fun addition — full-saturation `COLORS.success` (chartreuse), tried at 20% alpha first and full afterward per user request ("might be fun... looks badass"). This exposed `LinkCell`'s `COLORS.accentBlue` link text as a bad clash against chartreuse hover; swapped to `COLORS.accentPurple` after live comparison against pink (`COLORS.error`) — user picked purple. The header's column-separator investigation went through three iterations live (visible `#e0e0e0` line → toned-down dark low-alpha → fully disabled) before landing on `headerColumnBorder: false` per direct user request ("can you just make them invisible?"), after discovering AG's resize-handle indicator (`::after`, a short *centered* dash, not a full-height line) was a second, independent source of the same visual artifact.
**Follow-up:** Small unrelated cosmetics bundled into the same branch, verified via `getComputedStyle`/`.matches(':hover')` rather than screenshots after an earlier hover-underline claim turned out to be wrong on first check: header "?" button recolored (`COLORS.textMuted` → `COLORS.primary`, user's own live edit — was nearly invisible on the dark header); hover states added to the footer's three attribution links (underline, needed `!important` to beat antd's own Typography.Link specificity) and the "Select Columns"/"Download CSV"/"Feedback" controls (light background tint, color per-instance via CSS custom property so `index.css` itself stays free of literal color values) via two new global utility classes (`.link-hover-underline`, `.toolbar-btn-hover`); Feedback's hover tint set to `COLORS.error` then changed to `COLORS.textMuted` per user's own live edit. Also fixed `Footer.jsx`'s pre-existing (predates this branch, from #120) antd deprecation warning, `overlayInnerStyle` → `styles.container` — same fix pattern already used for `HowToUseModal` in #119. Console is now fully clean on load.

---
## 2026-07-12 — Fixed BoolCell checkmark invisible against new chartreuse row hover
**Issue:** #118
**Decision:** `ResortTable.jsx`'s `BoolCell` true-value checkmark changed from `COLORS.success` to `COLORS.bgHeader` (near-black). `COLORS.success` is the same chartreuse used for the new row-hover color added earlier in this same branch, so a hovered row rendered its checkmarks invisible (green glyph on green background) — caught by the user reviewing the branch before merge, not by any test or the earlier verification passes.
**Rationale:** Simplest fix that guarantees legibility on both the default white row and the chartreuse-hovered row, at the cost of losing the green "yes" color cue (the ✓/✗ glyph shapes still carry the meaning). Considered but rejected: a hover-specific CSS override to swap the checkmark color only while hovered — more moving parts for a cosmetic detail that's already solved by a single static color choice.
**Follow-up:** None — settled.

---
## 2026-07-13 — Unit selector (imperial/metric): manual toggle, formatter-based, collapsed table columns
**Issue:** #128
**Decision:** Discussed design up front and posted the resolution as a comment on #128 before writing code (full rationale there). Landed on: not a locale selector — manual, uniform, explicit toggle only, since locale doesn't reliably predict preference and ski culture already has cross-locale exceptions (vertical drop commonly stays in feet even at metric resorts). Control shape is a single compact button showing the *current* unit as a short text glyph (`ft`/`m`, no flags/globe icons — flags don't render reliably on Windows and are both US-centric and inaccurate as a unit-system metaphor), not a two-option `Segmented`, because mobile screen real estate is too tight for showing both options at once (the search bar already spans full width on mobile). Same shared state (`hooks/useUnits.js` context + `localStorage` persistence, split from `components/common/UnitsProvider.jsx` to satisfy the `react-refresh/only-export-components` lint rule) renders in two places — header (next to "?") and sidebar top row (left-aligned, next to "Reset all filters" which stays right-aligned) — reading/writing the same state directly, no read-only mirror. Implementation is formatter-based (`utils/units.js`: `convert*` for plain numbers, `format*` for numbers with a unit suffix) over the canonical imperial value from the API, not the pre-computed parallel-column pattern (`vertical_meters`, `trail_length_km` in the CSV are now unused by the frontend) — new unit-bearing fields never need a pipeline-side `_meters`/`_km` companion column. `ResortTable.jsx`'s `vertical`/`vertical_meters` and `trail_length_mi`/`trail_length_km` collapsed into single toggle-driven columns (AG Grid `context={{ unit }}` + `headerValueGetter`/`valueFormatter`, refreshed via `api.refreshHeader()`/`refreshCells({force:true})` on toggle) rather than staying as two independent always-optional columns — removes the redundancy the toggle exists to solve. `StatsFilters.jsx`'s Vertical/Trail Length XC sliders convert their displayed labels/values while filtering continues against canonical ft/mi server-side (display-only layer, filter contract unchanged). `HowToUseModal.jsx` documents the toggle; its `Btn` chip helper gained a `variant="primary"` (teal, matching the real button) since the default variant is pink and was misrepresenting the actual toggle color.
**Rationale:** Two live-review fixes applied after initial implementation: the sidebar toggle was grouped with "Reset all filters" via `marginLeft: auto`, reading as right-aligned in a narrow sidebar — moved to its own left-hand flex group so the row reads left-to-right (toggle+close-button, then Reset). Hectares confirmed correct as the metric acreage unit (not a typo/mix-up with "hectacre") — 1 ha = 10,000 m² = 2.471 ac, the standard metric land-area unit paired with acres.
**Follow-up:** None — settled. Verified live via Playwright at desktop and 390px mobile viewports: toggle stays in sync across header/sidebar/table/sliders/detail modal/map tooltip, and the preference survives a full reload via `localStorage`. Merged as PR #131 and deployed.

---
## 2026-07-13 — Data validation added to `Resort` Pydantic model
**Issue:** #83
**Decision:** Constraints added to `backend/models.py`'s `Resort` model only (the CSV-parsing boundary in `backend/data.py`) — `ResortSummary` is always built from an already-validated `Resort.model_dump()` (`main.py:279`), never parsed from raw input, so duplicate constraints there would validate a scenario that can't happen. Two-tier design: **hard** (raise) for missing required fields and a malformed `indy_page` (the one required URL, via `Field(pattern=...)`); **soft** (log a warning, coerce to `None`) for every optional numeric/URL field via `field_validator(mode='after')` — one glitchy scraped value nulls itself out rather than taking down the whole resort or the pipeline run. Bounds were set from an actual audit of `data/resorts.csv` (not guessed): non-negative fields (acres, num_trails(+_xc), num_lifts, vertical variants, snowfall, trail_length) got both a floor *and* a generous ceiling (e.g. acres 0–50,000 against an observed max of 19,136) to catch a scraped value with an accidental extra digit; difficulty percentages 0–100; Peak Rankings subscores 0–**11** (not 10 — Peak Rankings has actually issued a real 11, Spinal-Tap-style, so the ceiling accommodates it instead of nulling a legitimate score); `pr_total` floor only (no confident ceiling); PR ranks `ge=1`.
**Rationale:** Ceilings are calibrated to current real data with headroom, not a theoretical world-record max — the goal is catching obvious scraping glitches (10x errors), not constraining legitimate future resorts. Considered and rejected: applying the same constraints to `ResortSummary` (redundant, since it's never independently parsed); using `AnyUrl` for URL fields (normalizes/rewrites the string, e.g. adds a trailing slash to bare domains, which could subtly alter what's served — a plain regex `Field(pattern=...)`/soft-null keeps the string byte-for-byte as scraped); hard-failing the whole row on any out-of-range value (rejected per user direction — a single bad field on one resort shouldn't abort load or hide an otherwise-good resort). Verified against production data: all 277 current resorts load with zero fields nulled by the new bounds (bounds are accommodating enough for real data as it exists today).
**Follow-up:** #77 (scheduled pipeline) still needs its own design pass on how the soft-null warnings surface in the pre-PR review — e.g. a "N fields nulled across M resorts" summary in the PR description — since silent nulling means `load_resorts()` no longer raises on out-of-range values, only on structural failures (missing required fields, malformed `indy_page`). #132 (drop unused `vertical_meters`/`trail_length_km`) will remove some of these fields' constraints entirely once done — not done yet, low priority.

---
## 2026-07-13 — Scheduled pipeline workflow: manual dispatch, PR-based promotion, structural-only hard gate
**Issue:** #77
**Decision:** New `.github/workflows/pipeline-update.yml` runs the incremental pipeline in CI and promotes data via PR, never auto-commit. Trigger is `workflow_dispatch` only for now — the weekly cron gets added in a follow-up once the workflow has been exercised a few times. Sanity gate is a new `backend/validate_resorts.py` (lives in `backend/`, not `pipeline/`, because it imports `Resort`/`load_resorts` and only backend's env has pydantic): hard-fails the job (no PR) on structural problems — empty CSV, missing expected columns (checked against `Resort.model_fields`, catching silent parser column renames that per-row validation can't see), or a raised `ValidationError` from `load_resorts()`. Soft-nulled out-of-range fields (per #83's design) do **not** block; the script captures models.py's warning log via a logging handler and lists them in `reports/validation_summary.md`, which becomes the PR body ("N fields nulled — review before merging"). PR creation uses `peter-evans/create-pull-request@v6` with `add-paths: data/` (branch `automated/pipeline-data-update`, label `automated-data-update`); the action no-ops when nothing changed. A guard step reverts `data/pipeline_metadata.json` when it is the *only* changed file, so a no-change run doesn't open a timestamp-bump-only PR. A `full` boolean dispatch input passes `--full` to the pipeline; since `cache/` is gitignored, CI has no HTML cache and incremental mode already re-scrapes every resort page — in CI, "full" effectively means "also re-geocode all resort locations via the Google Maps API" (quota-sensitive; intended cadence ~quarterly, per user). The PR title and summary show which mode ran, and the expression (`inputs.full == true`) stays falsy on future `schedule` triggers, so cron runs will be incremental. Also added `reports/` to `.gitignore` (generated output, was never ignored despite CI writing there) and removed a stale tracked `reports/junit.xml` accidentally committed in December 2025.
**Rationale:** Hard-fail-on-any-soft-null was considered and rejected — it would revert the practical effect of #83's soft tier for the automated path; the human PR reviewer is the right judge of severity. Hand-rolled `gh pr create` scripting rejected in favor of the purpose-built action (diff detection, branch idempotency, PR update-in-place for free). Issue text said "any rows fail Pydantic validation via `load_resorts()`" — reconciled with #83's soft-null design as "any *raised* error", per user direction.
**Follow-up:** Add the weekly cron after a few successful manual runs. First real `workflow_dispatch` run succeeded post-merge (2026-07-13: 277 pages scraped in 4m12s, validation clean, no-change guard fired, no PR noise) — but that only exercises the no-change path. The data-changed→PR path is deliberately untested until live data changes (user decision: don't manufacture a change; full checklist of deferred verification items is in `docs/planning.md`). Real bug found during that review: the `automated-data-update` label the workflow references was never created in the repo — created 2026-07-13. Run annotations flag `actions/*@v4`/`create-pull-request@v6` as targeting deprecated Node 20 (both workflows) — trivial future version bumps. (`GOOGLE_MAPS_API_KEY` was added to the repo's Actions secrets by the user on 2026-07-13, so geocoding works in CI.) Investigating the `full` input's real value surfaced a bug: the geocode step is all-or-nothing (skips entirely when `resort_locations.csv` exists; regenerates everything under `--full`), so new resorts never get city/state/country in incremental runs — filed as #134 (bugfix: incremental behavior was the original intent). Once #134 lands, the "full refresh ~quarterly" guidance mostly evaporates and the `full` input becomes a rarely-needed escape hatch.

---
## 2026-07-14 — Split deferred pipeline verification into its own scheduled issue (#136)
**Issue:** #77, #136
**Decision:** #77's data-changed→PR path verification (checklist from the prior entry) moved off #77 and into new issue #136, targeted for ~early November 2026. #77 itself is merged and closed.
**Rationale:** User's original instruction was not to close #77 until the data-changed path was seen working live. Indy Pass resort data realistically won't shift until next-season info is published, historically Oct/Nov — holding #77 open until then would stall other delivery for months over a verification step that isn't actionable yet. Splitting it into a separately scheduled issue keeps #77 closed (matches actual completion state) without dropping the verification requirement.
**Follow-up:** Revisit #136 in early November, or sooner if source data changes are observed earlier.

---
## 2026-07-14 — Made geocoding incremental (fixes new resorts missing city/state/country)
**Issue:** #134
**Decision:** `generate_resort_locations_csv()` (`pipeline/utils.py`) now takes a `full: bool = False` param. By default it reads the existing `data/resort_locations.csv` (if present), builds a set of already-cached resort names, and only calls the Google Maps API for resorts not already in that set — new rows are appended to the existing cache rather than replacing it. `full=True` ignores the cache entirely and re-geocodes every resort, overwriting the file (unchanged escape-hatch behavior, just explicit now). `step_geocode()` (`pipeline/pipeline.py`) no longer skips the whole step when `resort_locations.csv` exists — it always calls `generate_resort_locations_csv(full=full)`, which is now a cheap no-op when nothing is missing.
**Rationale:** The bug (filed during #77 review): the old skip-if-exists-unless-full logic meant a brand-new resort added to the Indy Pass roster never got geocoded on an incremental run — it would render on the map (lat/lon come from the scraped page) but be invisible to country/state filters, with the only remedies being a full API-quota-burning re-geocode or a hand-edited CSV. Matching by `name` (not a stable ID) was accepted per the issue's own proposed fix — `resort_locations.csv` isn't keyed by the pipeline's UUID scheme.
**Follow-up:** `docs/ops-runbook.md` updated — the "full refresh ~quarterly" guidance is gone since new resorts now self-heal on every incremental run; `full=true` is now just a rarely-needed repair escape hatch. Repairing one bad cached entry (e.g. a wrong-country incident) is now: delete that resort's row from `resort_locations.csv`, run incrementally, only that resort gets re-geocoded.

---
## 2026-07-14 — Dropped unused vertical_meters/trail_length_km/acres_tt/vertical_tt fields
**Issue:** #132
**Decision:** Removed `vertical_meters` and `trail_length_km` (redundant metric-unit columns) and `acres_tt`/`vertical_tt` (tooltip display strings) from `backend/models.py` (`Resort` and `ResortSummary`, plus their `_NONNEG_BOUNDS` validator entries), from `pipeline/prep_resort_data.py`'s computation and final `cols` list, and regenerated `data/resorts.csv` (ran `prep_resort_data.py` locally against already-cached `blackout_dates_raw.csv`/`ltt_dates_raw.csv`/`peak_rankings_raw.csv`/`resort_locations.csv` — no live API calls). `feet_to_meters()` in `prep_resort_data.py` removed too, since `vertical_meters` was its only caller. `page_scraper.py`'s own `trail_length_km` — the value actually scraped from Indy's site (km is the source unit there; `trail_length_mi` is derived from it) — is untouched; that's a different thing from the redundant output column of the same name in `resorts.csv`.
**Rationale:** #128's unit toggle formats values client-side from canonical imperial fields, so these precomputed fields were dead weight on the wire and in the CSV with no reader left (confirmed via `grep` across `frontend/src` — zero matches for any of the four field names). Added a backend test (`test_unused_metric_and_tooltip_fields_are_dropped`) asserting the fields are absent from both Pydantic models, written first and confirmed failing before the removal, per this repo's TDD convention.
**Follow-up:** While confirming `acres_tt`/`vertical_tt` were dead, a broader grep turned up that every remaining `_tt` field (`location_name_tt`, `num_trails_tt`, `num_lifts_tt`, `pr_total_tt`) also has zero matches in `frontend/src` today — not just the two this issue named. Not acted on here to keep this change scoped to what #132 actually asked for; filed as #138 for separate verification and cleanup.

---
## 2026-07-14 — UI consistency pass: color tokens, field ordering, hide-when-null for XC-only stats
**Issue:** none (ad hoc polish on the #132 branch, done live against the dev server)
**Decision:** A grab-bag of small consistency fixes, done incrementally via Playwright-free live dev-server review:
- **Color:** unit picker (sidebar + header, `UnitToggle.jsx`), the header's "?" How-to-use button, and the How-to-use modal's unit description chips all recolored to `COLORS.error` (magenta) for a consistent accent; the modal's local `Btn` helper had its now-unused `isPrimary` branch removed since nothing passes `variant="primary"` anymore.
- **Field display order**, normalized across the map tooltip, sidebar `StatsFilters`, the resort table, and `ResortDetailModal` to: Vertical, Summit, Base / Acreage, Lifts, Trails, Trails (XC), Trail Length (XC) (renamed from "Trail Length XC" for parenthetical consistency with "Trails (XC)"). Table's `COL_GROUPS` "Size" list had already assumed this order (Acreage, Lifts, Trails...) while `COLUMN_DEFS` didn't match it — the two were quietly out of sync before this pass. Also reordered the table's Features/Reservations/Blackout columns (Allied moved next to the other feature toggles; Reservations, then Blackout Dates, then Learn to Turn, then Learn to Turn Blackouts) and `ResortDetailModal`'s Features grid (Dog Friendly before Snowshoeing, to match), adding `Allied` as a 7th feature (new 3rd grid row, lower-left cell, per user's explicit request over losing the clean 3×2 grid).
- **Hide-when-no-data, not just when-toggled-off:** `Trail Length (XC)`, `Acreage`, `Lifts`, `Summit`, and `Base` rows in the detail modal (and `Acreage`/`Lifts` in the map tooltip) now render conditionally on the field being non-null, rather than always showing a `—`/blank value — confirmed via data audit that all 43 XC-only resorts have null `num_lifts`/`acres`/`vertical_summit_ft`/`vertical_base_ft`, so this correctly hides those dimensions for XC-only resorts without needing a `has_alpine` check. This mirrors the existing pattern already used for the `Trails (XC)` row and the XC difficulty pie chart (`DifficultyChart` returns `null` when its inputs are all null).
- **Tooltip height/positioning bugfix caught along the way:** `ResortMap.jsx`'s `tooltipHeight()` assumed a fixed "6 or 7 rows" content shape to clamp the tooltip's position and avoid clipping — stale once Acreage/Lifts became conditionally hidden. Refactored so `buildTooltipRows(r, unit)` is the single source of truth for which rows render, used both by `MapTooltip` and by the hover handler's height calculation (`tooltipHeight(rowCount)`), so the two can't drift apart again.
- **Small lint/deprecation cleanup**, found via `npm run lint` and console warnings during this work: `HowToUseModal.jsx`'s hardcoded `'#ffffff'` replaced with `COLORS.bgBase` (project rule: no literal hex/rgba in `src/`); `FeatureFilters.jsx`'s deprecated `Button.Group` → `Space.Compact`; `BlackoutDateFilters.jsx`'s deprecated `Divider orientation`/`orientationMargin` → `titlePlacement`/`styles.content.margin`; `Header.jsx`'s dead `theme.useToken()` call removed.
**Rationale:** Started from a live-review question ("does anything else need reordering?") rather than a written spec — each change was proposed and confirmed live against the running dev server (`localhost:5173`) before moving to the next, per user's explicit calls (e.g. "vertical, summit, base... oddly intuitive to me" for the elevation order; "too bad we lose the 3×2 grid" accepted as the tradeoff for adding Allied).
**Follow-up:** Filed three issues surfaced during this pass rather than expanding scope further: #139 (no acreage filter exists despite acreage being shown prominently), #140 (Peak Rankings needs a Pass Affiliation filter — noted that its values are comma-separated combos, not clean single tags), #141 (broader `npm run lint` pre-existing findings: a real conditional-hook bug in `Footer.jsx`, a cluster of `setState`-in-effect issues across the filter components, and a couple other hard errors — none introduced by this session).

---
## 2026-07-17 — Add acreage (min_acres/max_acres) filter, with a log-scale slider track
**Issue:** #139
**Decision:** `acres` was the one stat shown in the map tooltip/table/detail modal without a matching range filter — added it following the exact pattern of the other numeric range filters. Backend: `min_acres`/`max_acres` query params on `GET /resorts` (`backend/main.py`), added to the `range_filters` list (null-`acres` resorts excluded, same as `trail_length_mi`); `acres: RangeField` added to `MetaResponse` and wired into `get_meta()`. Frontend: `min_acres`/`max_acres` added to `useFilters.js`'s parsed filter set; a new `Acreage` `RangeSlider` added to `StatsFilters.jsx`, placed between Vertical and Lifts to match the field-order convention established in the prior entry (Vertical → Acreage → Lifts → Trails → Trails (XC) → Trail Length (XC)). No `unitKind` — acreage isn't unit-converted like vertical/trail length.

Once live, the user immediately flagged the new Acreage slider as unusable: acreage ranges 9–19,136 across the dataset, but 83% of resorts sit under 1,000 acres (a handful of legitimately huge combined European resorts — Portes du Soleil ~16,000, an Innsbruck multi-resort network, a Pitztal-area combo ~19,000 — stretch a linear track out so far that most resorts were crammed into the first few pixels). Fixed with an optional `logScale` prop on `RangeSlider`: the antd `Slider`'s own position/drag space is remapped to `log10` via `toLogPos`/`fromLogPos` helpers (1000-step internal resolution, exact-snap to `metaMin`/`metaMax` at the track's own ends so "drag to the edge clears the filter" still works), while local state, the URL filter values, and the tooltip/label text all stay in real acreage units — only the track itself is log-mapped. Enabled on `Acreage` only. The same treatment was also tried on `Lifts` (also skewed, ≈42× max/p50 vs. acreage's ≈128×) but reverted after the user found the log-mapped drag "not smooth" there — `Lifts` stays linear.
**Rationale:** The filter itself was a straightforward gap-fill — the backend and frontend both already had a well-established range-filter pattern (5 other fields) to copy exactly. The log-scale slider was a reactive fix, not planned: confirmed the skew was real data (not a scraping artifact) by pulling percentiles from `data/resorts.csv` and identifying the actual resorts behind the outliers before concluding a log scale (not a bounds fix) was the right response. Whether log-scale reads as "smooth" seems to depend on the specific field/range, not just skew magnitude alone — Acreage's more extreme skew (128×) was judged worth the tradeoff live; Lifts's milder skew (42×) wasn't, so it's linear despite following the same math. Tests written first per TDD convention for the backend piece: `test_min_acres`/`test_max_acres`/`test_acres_range`/`test_acres_excludes_null_values` added to `backend/tests/test_resorts_feature_filters.py` (mirroring the existing `vertical` tests), plus an `acres` range assertion added to `backend/tests/test_meta_endpoint.py`'s numeric-ranges and empty-resorts tests — confirmed failing before implementation. The log-scale slider logic itself has no frontend test coverage (no component-testing library in this repo yet — only pure-function vitest tests exist), so it was verified live via Playwright instead, at multiple track positions on both the kept (Acreage) and reverted (Lifts) sliders.
**Follow-up:** None — verified live against dev servers: dragging Acreage/Lifts correctly filtered the map/table/count and synced `min_acres`/`max_acres`/`min_lifts`/`max_lifts` into the URL at sensible positions; zero new console errors or lint findings beyond the pre-existing #141 set.
